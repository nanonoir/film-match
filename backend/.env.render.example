# ========================================
# VARIABLES DE ENTORNO MÍNIMAS PARA RENDER
# ========================================
# Copia estas variables en tu servicio de Render (Settings > Environment)

# ===== REQUERIDAS (sin estas el backend NO funciona) =====

# Database URL - Copia desde Render PostgreSQL > Internal Database URL
DATABASE_URL=postgresql://film_match_user:password@dpg-xxx-a.oregon-postgres.render.com/film_match

# JWT Secrets - Genera strings aleatorios de mínimo 32 caracteres
# Puedes usar: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_min_32_characters_here
JWT_REFRESH_SECRET=your_super_secret_refresh_key_min_32_characters_here

# TMDB API Key - Obtén una gratis en https://www.themoviedb.org/settings/api
TMDB_API_KEY=your_tmdb_api_key_here

# Frontend URL - La URL de tu frontend en Vercel
FRONTEND_URL=https://your-app.vercel.app

# ===== CONFIGURACIÓN BÁSICA =====

NODE_ENV=production
PORT=3001
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=30d

# Google OAuth DESHABILITADO (para MVP)
GOOGLE_AUTH_ENABLED=false

# ===== OPCIONALES (el backend funciona sin ellas) =====

# Gemini API - Para chat con IA (si no la tienes, el chatbot usará fallback)
# Obtén una gratis en: https://aistudio.google.com/app/apikey
# GEMINI_API_KEY=your_gemini_api_key_here

# Pinecone - Para búsqueda semántica avanzada (no crítico para MVP)
# PINECONE_API_KEY=your_pinecone_api_key_here
# PINECONE_ENVIRONMENT=us-east-1
# PINECONE_INDEX=film-match

# ========================================
# NOTAS IMPORTANTES
# ========================================
#
# 1. NO uses comillas en los valores en Render (ej: JWT_SECRET=abc123 NO "abc123")
# 2. DATABASE_URL debe ser la URL INTERNA de Render (dpg-xxx-a.oregon...)
# 3. Después de agregar/cambiar variables, haz un Manual Deploy
# 4. Si habilitas Google OAuth (GOOGLE_AUTH_ENABLED=true), necesitarás:
#    - GOOGLE_CLIENT_ID
#    - GOOGLE_CLIENT_SECRET
#    - GOOGLE_REDIRECT_URI=https://your-backend.onrender.com/api/auth/google/callback
